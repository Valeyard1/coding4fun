#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    int entradas[3];

    for(int i = 0;i < 3;i++) {
        cin >> entradas[i];
    }
    sort(entradas,entradas+3);

    i = 1;
    if(entradas[i - 1] > entradas[i])

    if(entradas[i] > entradas[i+1])
    return 0;
}
